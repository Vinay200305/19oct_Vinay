#include<stdio.h>
int main()
{
    int no;
    printf("enter the number:");
    scanf("%d",&no);
    if(no>0)
    {
        printf("the number is positive.");
    }
    else
    {
        printf("the number is negative.");
    }
    return 0;
}
